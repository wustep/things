# Optional. When set, `npm run ingest` also requests an image-to-3D mesh from Meshy
# (https://www.meshy.ai) and stores it as public/items/<id>/model.glb.
# Without it, ingest produces a fully offline procedural asset.
MESHY_API_KEY=
